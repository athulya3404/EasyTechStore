volumes:
  electronics_shopping_website_local_postgres_data: {}
  electronics_shopping_website_local_postgres_data_backups: {}
  

services:
  django:
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: electronics_shopping_website_local_django
    container_name: electronics_shopping_website_local_django
    depends_on:
      - postgres
      - mailpit
    volumes:
      - /app/.venv
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - '8000:8000'
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: electronics_shopping_website_production_postgres
    container_name: electronics_shopping_website_local_postgres
    volumes:
      - electronics_shopping_website_local_postgres_data:/var/lib/postgresql/18/docker
      - electronics_shopping_website_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres

  mailpit:
    image: docker.io/axllent/mailpit:latest
    container_name: electronics_shopping_website_local_mailpit
    ports:
      - "8025:8025"
